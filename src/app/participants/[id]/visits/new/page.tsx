'use client';

// This page just renders the layout's visit type selector.
// Once a visit type is selected, the layout redirects to /clinical.
export default function WizardIndexPage() {
    return null;
}
