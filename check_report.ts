import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const rawData = await prisma.crfLabResult.findMany({ include: { analyte: true, visit: { select: { id: true, visitType: true, visitDate: true, participant: { select: { studyId: true } } } } }, orderBy: { createdAt: 'asc' } }); console.log('Raw data count:', rawData.length); if (rawData.length > 0) console.log(rawData[0]); } main().finally(() => prisma.$disconnect());
